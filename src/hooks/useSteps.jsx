import React from "react";

const STEPS_INFO = {
  0: "Src & Target Upload",
  1: "Field Selection",
  2: "Src & Target Mapping",
};

const useSteps = () => {
  return STEPS_INFO;
};

export default useSteps;
