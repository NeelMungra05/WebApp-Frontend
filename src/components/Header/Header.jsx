import React from "react";

const Header = (props) => {
  return (
    <header>
      <nav>
        <ul>
          <li>Login</li>
          <li>Register</li>
        </ul>
      </nav>
    </header>
  );
};

export default Header;
